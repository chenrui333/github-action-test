name: misc-formulae-bump

on:
  schedule:
    # run daily 10:00AM EST
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  bump:
    runs-on: macos-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Bump formulae
        uses: dawidd6/action-homebrew-bump-formula@v3
        continue-on-error: true
        with:
          token: ${{secrets.REPO_TOKEN}}
          livecheck: true
        env:
          HOMEBREW_LIVECHECK_WATCHLIST: formulae-misc
