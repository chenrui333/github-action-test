name: zap-scan meetup.com/lp/meetup-pro

on:
  push:
    paths:
      - .github/workflows/zap-lp-pro.yml
  workflow_dispatch:

jobs:
  zap_scan:
    runs-on: ubuntu-latest
    name: Scan ZAP website
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: master
      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.3.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target: 'https://www.meetup.com/lp/meetup-pro'
          rules_file_name: '.github/workflows/zap-rules/rules.tsv'
