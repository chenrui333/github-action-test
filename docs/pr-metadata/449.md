## Purpose

Inspect the raw hosted runner with an interactive shell before explicitly installing Homebrew.

## Environment

- Runner: `ubuntu-22.04-arm`
- Architecture: ARM64
- Head inspected: `955c5c72a0f1` (audit snapshot, September 5, 2026)
- Tool versions: image-dependent; record `brew --version`, image version, and action pins with each run.

## What this tests

Current head only checks out and opens Upterm; the historical custom installer call was removed.

## Current status

Old debug job succeeded, but this is not a formula-build assertion. Branch conflicts with main.

## Reproduction / validation

Inspect this branch's `.github/workflows/brew-debug.yml` and its latest Actions run.
Do not equate a shell session's conclusion with a formula test. Use the manual
Runner diagnostics workflow after #756 lands for routine platform inspection.

## Exit criteria

Record initial brew availability, image version and the concrete observation; pin actions and bound the session before rerunning.

## Related

- #639
- #756
