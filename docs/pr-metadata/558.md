## Purpose

Inspect Homebrew on the macOS 26 hosted image.

## Environment

- Runner: `macos-26`
- Architecture: ARM64
- Head inspected: `d284c5e6958b` (audit snapshot, September 5, 2026)
- Tool versions: image-dependent; record `brew --version`, image version, and action pins with each run.

## What this tests

Runs brew update from the assumed core checkout, then opens Upterm.

## Current status

Latest debug job was cancelled; no failure diagnosis follows from cancellation.

## Reproduction / validation

Inspect this branch's `.github/workflows/brew-debug.yml` and its latest Actions run.
Do not equate a shell session's conclusion with a formula test. Keep this
branch as the reproduction and record the image version and commands with each run.

## Exit criteria

Record the image version, specific macOS 26 observation and exit criteria. Pin actions and bound the session before another run.

## Related

- #636
- #644
- #756 (scratchpad template hardening)
