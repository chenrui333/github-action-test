## Purpose

Inspect Homebrew on an Intel macOS 15 runner.

## Environment

- Runner: `macos-15-intel`
- Architecture: x64
- Head inspected: `0ce4d3011b94` (audit snapshot, September 5, 2026)
- Tool versions: image-dependent; record `brew --version`, image version, and action pins with each run.

## What this tests

Runs brew update from the Intel core checkout, then opens tmate v3.

## Current status

Debug job cancelled; branch is conflicted and still uses mutable tmate.

## Reproduction / validation

Inspect this branch's `.github/workflows/brew-debug.yml` and its latest Actions run.
Do not equate a shell session's conclusion with a formula test. Use the manual
Runner diagnostics workflow after #756 lands for routine platform inspection.

## Exit criteria

Validate #756 on Intel; preserve any observation before retiring the branch experiment.

## Related

- #636
- #756
