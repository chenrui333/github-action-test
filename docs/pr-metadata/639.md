## Purpose

Measure core-tap cache reuse with supported Homebrew setup and a source build.

## Environment

- Runner: `ubuntu-22.04-arm`
- Architecture: ARM64
- Head inspected: `889c31d4cc00` (audit snapshot, September 5, 2026)
- Tool versions: image-dependent; record `brew --version`, image version, and action pins with each run.

## What this tests

Restore core; setup-homebrew core=true; explicitly enable Git-tap installs; source-build hello and check version/linkage.

## Current status

February checks succeeded; logs expired (HTTP 410), so cold/warm cache effectiveness is unverified. Branch conflicts with main; checkout/cache tags and persisted checkout credentials need repair.

## Reproduction / validation

Inspect this branch's `.github/workflows/brew-debug.yml` and its latest Actions run.
Do not equate a shell session's conclusion with a formula test. Use the manual
Runner diagnostics workflow after #756 lands for routine platform inspection.

## Exit criteria

Refresh in an isolated cache workflow; demonstrate cold and warm runs, an updated cache generation, and correct Git-tap behavior. Keep the explicit NO_INSTALL_FROM_API assignments: setup clears that variable.

## Related

- #449
- #755
- #756
